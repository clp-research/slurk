[tool.autopep8]
max_line_length = 120
in-place = true
recursive = true
aggressive = 3

[tool.pytest.ini_options]
testpaths = "tests/"
filterwarnings = ["ignore::DeprecationWarning", "ignore:Unverified HTTPS request"]
